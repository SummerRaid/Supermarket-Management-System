<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sms.mapper.ProductMapper">
    <resultMap id="productResultMap" type="product">
        <association property="order" column="order_id" select="sms.mapper.OrderMapper.selectById"/>
        <association property="shop" column="supermarket_id" select="sms.mapper.ShopMapper.selectById"/>
    </resultMap>
    <insert id="add" useGeneratedKeys="true" keyProperty="id">

    </insert>
    <update id="update" useGeneratedKeys="true" keyProperty="id">

    </update>
    <delete id="del">

    </delete>

    <select id="selectAll" resultMap="productResultMap">

    </select>
    <select id="selectByName" resultMap="productResultMap">

    </select>
    <select id="selectByOrder" resultMap="productResultMap">

    </select>
    <select id="selectById" resultMap="productResultMap">

    </select>
</mapper>